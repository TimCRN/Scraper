name: Python package

on: push

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.8"]

    steps:
      - uses: actions/checkout@v3
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flake8 pytest bs4 requests pandas selenium tqdm time datetime
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
      - name: schedulebuild
        run: |
          python ee.py

# on:
#   schedule:
#     - cron: "8 0 * * *" #runs at 08:00 UTC everyday

# jobs:
#   build:

#     runs-on: ubuntu-latest
#     strategy:
#       matrix:
#         python-version: ["3.7", "3.8", "3.9", "3.10"]

#     steps:
#       - uses: actions/checkout@v3
#       - name: Set up Python ${{ matrix.python-version }}
#         uses: actions/setup-python@v4
#         with:
#           python-version: ${{ matrix.python-version }}
#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install flake8 pytest bs4 requests pandas selenium os tqdm time datetime
#           if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
#       - name: schedulebuild
#         run: |
#           python ee.py
